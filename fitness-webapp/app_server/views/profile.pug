extends layout

block content
    h1= title
    table.table
        thead
            tr
                th(scope="col") Username
                th(scope="col") Id
                th(scope="col") Hash
        tbody
            each user in users
                tr
                    td=user.username
                    td=user._id
                    td=user.hash


                