extends layout

block content
    table.table
        thead
            tr
                th(scope="col") Username
                th(scope="col") Id
                th(scope="col") Hash
        tbody
            tr
                td=user.username
                td=user._id
                td=user.hash


                