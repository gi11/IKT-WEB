extends layout
include workout/exerciselist
block content
    .card
        .card-header
            p 
                = workout.name
            span 
                = workout.description
        .card-body
            +ExerciseList(workout.exercises, workout.id)